# 작업 진행 상황

## 전체 과제 구현 계획

상태: PRD 작성 및 기능별 로컬 브랜치 생성 완료 / 신규 기능 구현 미착수

- 전체 요구사항과 브랜치별 구현 순서: [PRD](PRD.md)
- PRD-002~007: 공통 CLI, 카테고리 관리, 거래 검색, 월별 요약, 월 예산, CSV 입출력
- 신규 브랜치 6개는 `develop`의 `870c609`에서 생성했다.
- 구현 전 선행 기능이 포함된 최신 `develop`을 각 기능 브랜치에 반영한다.
- 현재 작업은 문서와 브랜치 준비이며 애플리케이션 기능은 변경하지 않았다.

### 확인 대기

- 옵션 접두사: `AGENTS.md`·과제 제약사항의 `-`와 기존 PRD·상세 요구사항의
  `--`가 상충한다. 사용자 확인 후 전체 문서와 CLI 구현 기준을 확정한다.
- 신규 PRD의 `--` 명령 예시는 기존 표기를 사용한 초안이다.

### 이번 작업의 검증

- `uv run python -m unittest discover -v`: 기존 테스트 29개 통과
- `uv run ruff format .`: 25개 파일 변경 없음
- `uv run ruff check .`: 통과
- Ruff는 PATH에 없어 기존 uv 캐시의 실행 파일을 PATH에 추가하여 실행했다.
  의존성 추가나 새 패키지 설치는 하지 않았다.
- 위 테스트 결과는 기존 구현 대상이며 신규 기능의 완료 증거는 아니다.

### 다음 작업

1. CLI 표기 확인 결과를 PRD에 반영한다.
2. [PRD-002](PRD-002.md)의 공통 실행 기반부터 순서대로 구현한다.
3. 각 기능의 수용 조건·관련 테스트·전체 검사·README를 함께 완성한다.

## PRD-001 Transaction 기능

상태: 구현 완료

### 완료된 작업

- `Transaction` 모델 및 JSON 직렬화·역직렬화
- 거래 JSONL 추가·조회·수정·삭제 저장소
- 거래 ID 생성 및 CRUD 서비스
- `add`, `list`, `update`, `delete` CLI
- 날짜, 타입, 금액 입력 검증
- 등록 카테고리 확인 및 미등록 카테고리 등록·재입력 흐름
- 카테고리 이름의 별도 JSONL 저장

### 검증 결과

- `python -m unittest discover -v`: 29개 통과
- `ruff format .`: 통과
- `ruff check .`: 통과

### 남은 작업

- 없음
